<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_layout"
    android:layout_width="200dp"
    android:layout_height="220dp"
    android:scaleX=".92"
    android:scaleY=".7"
    android:background="@drawable/categories_background"
    android:elevation="5dp"
    >

    <ImageView
        android:id="@+id/cat_pic"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/cat_15"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="50dp"
        android:layout_marginRight="0dp"
        android:background="@drawable/background_category"
        android:elevation="3dp"
        />
    <TextView
        android:id="@+id/cat_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:text="Pizza"
        android:textSize="25sp"
        app:layout_constraintTop_toBottomOf="@+id/cat_pic"
        app:layout_constraintStart_toStartOf="@+id/cat_pic"
        app:layout_constraintEnd_toEndOf="@+id/cat_pic"
        android:textColor="#FF9800"
        android:textStyle="bold"
        />

</androidx.constraintlayout.widget.ConstraintLayout>